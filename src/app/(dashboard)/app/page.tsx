/**
 * Dashboard page for the user.
 * If the user is an admin, more details should be shown.
 */
export default async function App() {
  // Fetch the data and show the component renders each with a suspense fallback
  return (
    <main className="w-full flex min-h-screen flex-col items-center justify-between p-8">
      {/* Dashboard page */}
      
    </main>
  );
}
